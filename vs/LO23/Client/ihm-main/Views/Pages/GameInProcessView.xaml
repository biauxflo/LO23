<Page x:Class="Client.ihm_main.Views.Pages.GameInProcessView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.ihm_main.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GameInProcessView">
    <Page.Resources>
        <DataTemplate x:Key="GameInProcess_Template">
            <StackPanel Background="Gray" Width="auto" Height="auto">
                
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Partie en cours" FontSize="30" FontWeight="Bold" TextAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <Button x:Name="BT_CreateGame" Content="Créer une partie" Background="Gray" Width="200" Margin="10 10 70 10"/>
            <Button x:Name="BT_ReviseBackup" Content="Revisionner une sauvegarder" Background="Gray" Width="200" Margin="70 10 10 10"/>
        </StackPanel>
        
    </Grid>
</Page>
